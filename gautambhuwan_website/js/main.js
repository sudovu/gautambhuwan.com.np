/**
 * VHUWON MATHERS — Master Entrypoint
 */

document.addEventListener('DOMContentLoaded', () => {
  // Initialize Theme System
  if (window.AppData) {
    window.AppData.initTheme();
  }

  console.log("VHUWON MATHERS | Digital Infrastructure Portfolio Initialized.");
});
